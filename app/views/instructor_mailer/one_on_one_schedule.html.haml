%p
  Here's your one-on-one schedule

%table
  %thead
    %tr
      %th Name
      %th Date
      %th Time
  %tbody
    - @appointments.each do |appointment|
      %tr
        %td= appointment.student.full_name
        %td #{appointment.date.strftime('%A')}, #{appointment.date.strftime('%-m/%-d/%Y')}
        %td= appointment.time
